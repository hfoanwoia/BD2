services:
  - type: web
    name: influencer-crm-admin
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: INTEGRATION_MODE
        value: mock
      - key: DATABASE_PATH
        value: /tmp/commerce.db
